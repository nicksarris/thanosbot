import React from 'react';
import './index.css';

const SnapText = (props) => (
  /* Final Rendered Component/States */
  <div className="snapText">
    {props.children}
  </div>
)

export default SnapText;
